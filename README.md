# loan_approval

Loan Approval Prediction
A simple Python program that uses machine learning (Decision Tree) to predict if a loan application will be approved or rejected.

Features
Generates a sample dataset (loan.csv) if not present.

Encodes categorical values like Employment Status and Marital Status.

Trains a Decision Tree model to classify loan outcomes.

Takes applicant details via terminal input and displays the approval decision.
